# Getting Started

Refer to [the overview](../overview.md) for background — this file
does NOT exist (stale link: should be caught).

See [README](../../README.md) — this DOES exist (should NOT be caught).

Config lives at `src/myapp/config.py` — this should resolve to repo root
and be found (NOT a stale path).
