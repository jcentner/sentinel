[project]
name = "myapp"
version = "0.2.0"
dependencies = [
    "click>=8.0",
    "httpx>=0.27",
    "sqlalchemy>=2.0",
    "bcrypt>=4.0",
    "pydantic>=2.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "ruff>=0.4",
]
